{%- css %}.postlist { counter-reset: start-from {{ (0) }} }{% endcss %}
<ol class="postlist">
{% for post in postslist %}
	<li class="postlist-item">
		<a href="{{ post.url }}" class="postlist-link">{{ post.data.title }}</a><hr>
		{{ post.data.description }}
	</li>
{% endfor %}
</ol>
